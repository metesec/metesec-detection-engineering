{
  "schema_version": 1,
  "id": "MSEC-DET-0085",
  "title": "Netsh helper DLL registered from a staging or remote path",
  "description": "Detects a machine registry DLL registration whose complete value names a DLL in selected profile, temporary, environment-variable or UNC locations.",
  "hypothesis": {
    "summary": "An adversary may register an untrusted Netsh helper DLL to obtain execution when the utility loads its helpers.",
    "rationale": "The registry write does not prove the DLL exists, is malicious or was loaded. Relative DLL names and other locations are outside this deliberately bounded predicate.",
    "detection_goal": "Detects a machine registry DLL registration whose complete value names a DLL in selected profile, temporary, environment-variable or UNC locations."
  },
  "ownership": {
    "owner": "MeteSec Detection Engineering",
    "reviewers": []
  },
  "lifecycle": {
    "status": "experimental",
    "created": "2026-09-11",
    "modified": "2026-09-11",
    "review_interval_days": 90
  },
  "severity": "high",
  "confidence": "medium",
  "data_sources": [
    {
      "name": "Microsoft Defender for Endpoint registry events",
      "category": "Endpoint registry modification",
      "required_fields": [
        "TimeGenerated",
        "Timestamp",
        "DeviceId",
        "DeviceName",
        "ActionType",
        "InitiatingProcessAccountDomain",
        "InitiatingProcessAccountName",
        "InitiatingProcessAccountSid",
        "InitiatingProcessFileName",
        "InitiatingProcessCommandLine",
        "RegistryKey",
        "RegistryValueName",
        "RegistryValueData",
        "ReportId"
      ]
    }
  ],
  "attack": [
    {
      "technique_id": "T1546.007",
      "tactic": "Persistence"
    }
  ],
  "triage": {
    "summary": "The registry write does not prove the DLL exists, is malicious or was loaded. Relative DLL names and other locations are outside this deliberately bounded predicate.",
    "steps": [
      "Review the exact registry key, complete DLL path, account and process responsible for the change.",
      "Inspect DLL hash, signature, owner and provenance; check relevant process image-load events and subsequent execution.",
      "Confirm the approved networking, print or compatibility requirement and investigate unexplained persistence changes."
    ],
    "false_positives": [
      "Authorized vendor networking, diagnostic or compatibility software registering a helper from a nonstandard path."
    ]
  },
  "validation": {
    "expected_outcome": "Match the bounded event, key/action/path or successful operation combination; reject one-field mutations, lookalikes and absent required fields. Synthetic tests do not establish live telemetry or production performance.",
    "positive_tests": true,
    "negative_tests": true
  },
  "implementations": [
    {
      "type": "sigma",
      "path": "content/portable/sigma/MSEC-DET-0085/rule.yml",
      "targets": [
        "sentinel"
      ],
      "status": "active"
    }
  ],
  "quality": {
    "reviewed_on": "2026-09-11",
    "signal_type": "configuration-change",
    "limitations": [
      "The registry write does not prove the DLL exists, is malicious or was loaded. Relative DLL names and other locations are outside this deliberately bounded predicate.",
      "Current-revision evidence is synthetic only. Validate full generated KQL, source fields, ingestion delay, alert volume and scoped exclusions in the target before enabling."
    ],
    "validation_level": "synthetic-only",
    "provenance": {
      "origin": "reworked-supplied",
      "license": "Apache-2.0",
      "review": "docs/reviews/candidates-0051-0100.md"
    }
  }
}
